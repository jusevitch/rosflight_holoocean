# ROS 2 build artifacts
/build/
/install/
/log/

# ROSflight SIL board persistent memory (created when running the sim)
/rosflight_memory/

# ROSflight source repositories — cloned by scripts/setup_workspace.sh.
# They are kept out of this repo so it stays a lightweight, reproducible
# DevPod template. Run the setup script (or `devpod up`) to populate them.
/src/rosflight_ros_pkgs/
/src/rosplane/
/src/roscopter/

# HoloOcean repository — cloned by scripts/setup_holoocean.sh under the
# user's own Epic-linked GitHub credentials. NEVER commit its contents here:
# it is governed by the Unreal Engine EULA and may only be distributed to
# Engine Licensees.
/holoocean/

# Claude Code local (user-specific) settings
.claude/settings.local.json

# Editor / OS cruft
*.swp
*.swo
*~
.DS_Store
.idea/
